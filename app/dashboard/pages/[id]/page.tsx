import {notFound} from 'next/navigation';import {createClient} from '../../../../lib/supabase/server';import PageEditor from '../../../../components/PageEditor';
export default async function EditPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const db=await createClient();const {data}=await db.from('page_content').select('*').eq('id',id).maybeSingle();if(!data)notFound();return <><div className="page-heading"><div><span className="eyebrow">Page editor · {data.route}</span><h1>Edit {data.name}</h1><p>Update content, then save as a draft or publish it on the public website.</p></div></div><PageEditor page={data}/></>;}
