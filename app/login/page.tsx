import LoginForm from '../../components/LoginForm';
export default function LoginPage() { const hasSupabaseConfig = Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL && (process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY)); return <main className="auth-wrap"><div className="auth-card"><img src="/riloc-logo.png" alt="RILOC" width="160"/><span className="eyebrow">Administrator sign in</span><h1>Welcome back</h1><p>Sign in with the administrator account created in Supabase.</p>{!hasSupabaseConfig&&<div className="notice notice-error">Supabase isn’t connected to this Vercel deployment yet. Add <code>NEXT_PUBLIC_SUPABASE_URL</code> and <code>NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY</code> to the dashboard project’s Environment Variables, then redeploy.</div>}<LoginForm/></div></main>; }
