export default function SetupPage(){return <main className="auth-wrap"><div className="setup-card"><span className="eyebrow">One-time setup</span><h1>Connect your protected dashboard</h1><p>Create a Supabase project, then add its Project URL and publishable key to <code>.env.local</code>.</p><ol><li>Run <code>supabase/schema.sql</code> in the Supabase SQL Editor.</li><li>Create an administrator user in Supabase Authentication.</li><li>Copy the user ID. Set <code>SUPABASE_SECRET_KEY</code> and <code>SUPABASE_ADMIN_USER_ID</code> in <code>.env.local</code>, then run <code>npm run seed</code>.</li><li>Set the public website environment variables to the same project.</li></ol><p>After setup, return here and refresh to sign in.</p></div></main>;}
